---
import { loadArticles, loadProjects } from '@/lib/cms'
import type { Language } from '@/i18n/ui'
import PortfolioHome from '@/components/PortfolioHome.astro'

const lang: Language = 'en'
const projects = await loadProjects('en')
const notes = (await loadArticles('en'))
  .sort((a, b) => b.data.publishedAt.localeCompare(a.data.publishedAt))
---

<PortfolioHome lang={lang} projects={projects} notes={notes} />
