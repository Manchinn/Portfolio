---
interface Props {
  children?: string
  as?: 'h1' | 'h2' | 'h3'
  class?: string
}
const { as = 'h2', class: className = '' } = Astro.props
const Tag = as
---

<Tag class={`display text-[var(--hw-text-h2)] text-ink ${className}`}>
  <slot />
</Tag>
