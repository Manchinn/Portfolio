---
import type { Language } from '@/i18n/ui'
import { getUI } from '@/i18n/ui'
import { PUBLIC_CONTACT_URL } from '@/data/types'
import SectionLabel from '@/components/ui/SectionLabel.astro'
import Button from '@/components/ui/Button.astro'

interface Props {
  lang: Language
}
const { lang } = Astro.props
const ui = getUI(lang)
---

<section id="contact" class="mx-auto max-w-[1200px] px-5 py-16 sm:px-8">
  <div class="card grid gap-8 p-8 sm:p-12 lg:grid-cols-[1.2fr_0.8fr] lg:items-center">
    <div>
      <SectionLabel>{ui.contactEyebrow}</SectionLabel>
      <h2 class="mt-4 text-2xl font-semibold text-ink sm:text-3xl">{ui.contactTitle}</h2>
      <p class="mt-4 max-w-[52ch] leading-relaxed text-muted">{ui.contactBody}</p>
    </div>
    <div class="flex flex-col items-start gap-4">
      <Button href={PUBLIC_CONTACT_URL} external={true}>
        {ui.contactAction}
      </Button>
      <p class="text-xs leading-relaxed text-muted">{ui.contactNotice}</p>
    </div>
  </div>
</section>
