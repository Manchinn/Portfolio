---
import type { Language } from '@/i18n/ui'
import { getUI } from '@/i18n/ui'
import { PUBLIC_CONTACT_URL } from '@/data/types'
import Reveal from '@/components/motion/Reveal.astro'
import Button from '@/components/ui/Button.astro'

interface Props {
  lang: Language
}
const { lang } = Astro.props
const ui = getUI(lang)
const base = lang === 'en' ? '' : '/th'
---

<section id="home" class="grain relative grid w-full gap-10 px-[var(--hw-gutter)] py-[calc(96*var(--u))] md:grid-cols-[1.15fr_0.85fr] md:items-center">
  <div>
    <Reveal>
      <span class="eyebrow">{ui.heroEyebrow}</span>
    </Reveal>
    <Reveal delay={80}>
      <h1 class="display mt-5 text-[var(--hw-text-display)] text-ink">
        {ui.heroTitle}
      </h1>
    </Reveal>
    <Reveal delay={160}>
      <p class="mt-6 max-w-[52ch] text-[var(--hw-text-body)] leading-relaxed text-muted">{ui.heroBody}</p>
    </Reveal>
    <Reveal delay={240}>
      <div class="mt-8 flex flex-wrap gap-3">
        <Button href={`${base}/#work`}>{ui.heroPrimaryCta}</Button>
        <Button href={PUBLIC_CONTACT_URL} variant="secondary" external={true}>
          {ui.heroSecondaryCta}
        </Button>
      </div>
    </Reveal>
  </div>

  <Reveal delay={320}>
    <div class="card card-outline p-[calc(32*var(--u))]">
      <p class="eyebrow">System</p>
      <ul class="mt-5 space-y-4">
        {ui.heroMeta.map((item) => (
          <li class="flex items-start justify-between gap-4 border-b border-line pb-4 last:border-0 last:pb-0">
            <span class="text-sm font-medium text-ink">{item.label}</span>
            <span class="font-mono text-sm text-muted">{item.value}</span>
          </li>
        ))}
      </ul>
    </div>
  </Reveal>
</section>
